var templateBuild = require('../../templateBuild');
var defaultItems = require('../../buildUtilities/demoData');
var assert = require('assert');

describe('buildUtilities sizePosterFrames() test', function () {


    beforeEach(function () {
        // runs before each test in this block
    });

    afterEach(function () {
        // runs after each test in this block
    });


    // Tests
    describe('sizePosterFrames()', function () {
        it('sizes poster frames correctly', async function () {
        });
    });

});

