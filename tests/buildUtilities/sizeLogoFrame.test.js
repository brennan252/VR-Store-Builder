var templateBuild = require('../../templateBuild');
var defaultItems = require('../../buildUtilities/demoData');
var assert = require('assert');

describe('buildUtilities sizeLogoFrame() test', function () {


    beforeEach(function () {
        // runs before each test in this block
    });

    afterEach(function () {
        // runs after each test in this block
    });


    // Tests
    describe('sizeLogoFrame()', function () {
        it('sizes LogoFrame correctly', async function () {
        });
    });

});

